- name: Instalar y arrancar Apache en Ubuntu
  hosts: web
  become: yes

  tasks:
    - name: Instalar Apache
      apt:
        name: apache2
        state: present
        update_cache: yes

    - name: Asegurar que Apache está iniciado
      service:
        name: apache2
        state: started
        enabled: yes
